---
tags:
  - 领域
  - Git
  - 工作流
type: note
---

# Git 工作流

## 这是什么

这篇笔记不讲零散命令，而是讲我在日常开发里应该按什么顺序使用 Git，才能少踩坑、少冲突、少慌张。

它更像一篇经验文，重点是“什么时候该做什么”，而不是“命令大全”。

## 为什么重要

- 很多 Git 问题不是不会命令，而是顺序错了
- 同步、查看、暂存、提交、推送这些动作如果混着来，出错概率会明显上升
- 一套稳定工作流能减少不确定性，也更方便协作

## 最小工作流

一个最常见的日常循环可以先理解成：

1. 先同步最新代码
2. 在当前分支上修改文件
3. 查看变更
4. 暂存
5. 提交
6. 推送

对应命令通常是：

```bash
git pull --rebase origin main
git status
git add .
git commit -m "说明这次修改"
git push origin main
```

## 如果是做新功能

我更推荐这样走：

1. 先更新主分支
2. 从主分支切新分支
3. 在功能分支开发
4. 提交并推送功能分支
5. 再去合并

```bash
git checkout main
git pull --rebase origin main
git checkout -b feature/xxx
git status
git add .
git commit -m "实现 xxx"
git push --set-upstream origin feature/xxx
```

## 这套顺序为什么有用

我现在会这样理解：

- `pull` 是先把远程最新内容同步下来
- `status` 是先看清现场
- `add` 是确定哪些内容准备提交
- `commit` 是把这次修改写进本地历史
- `push` 是把本地结果发到远程

这套顺序的核心价值是：每一步都在降低不确定性。

## 提交前我会检查什么

- 当前是不是站在对的分支上
- 有没有不该提交的文件
- 提交信息是不是能说明“这次改了什么”

## 一个最常见的错误场景

很多人会一上来就改文件，最后准备提交时才发现：

- 没同步最新代码
- 站错分支
- 把临时文件也一起加进去了

这类问题的解决办法往往不是记更多命令，而是把工作流顺序固定下来：先同步、再看状态、再提交。

## 容易出问题的几个场景

### 1. 没同步就直接写

结果：

- 推送时容易冲突
- 甚至会误覆盖别人改动

### 2. 什么都 `git add .`

结果：

- 可能把临时文件、测试文件、无关修改一起提交

更稳妥的做法通常是提交前先看一次 `git status`。

### 3. 提交信息太空

比如：

- `update`
- `修改`
- `test`

这种信息过几天基本等于没写。

更好的写法是：

- `补充 Git 工作流笔记`
- `重构数组主题结构`
- `新增二分查找模板`

## 易错点

- 容易把 Git 当成“想到什么命令就敲什么”，而不是一条有顺序的流程
- 容易在没确认分支和状态前就直接提交
- 容易把 `git add .` 当默认动作，却不检查范围

## 我的理解

Git 工作流真正有用的地方，不是命令本身，而是它在帮我把“同步、确认、提交、推送”这些动作排成稳定顺序。

一旦顺序稳定了，很多 Git 焦虑其实会明显下降。

## 相关笔记

- [[Git 基础命令]]
- [[Git 与 GitHub 总览]]
